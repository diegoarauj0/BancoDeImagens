<form action="/auth/login" method="post" autocomplete="on">
  <div>
    {{#if validationErrors.email}}
      <div class="error"><p>{{validationErrors.email.message}}</p></div>
    {{/if}}
    <label for="email">Email:</label>
    <input type="email" name="email" id="email" autocomplete="email" required>
  </div>

  <div>
    {{#if validationErrors.password}}
      <div class="error"><p>{{validationErrors.password.message}}</p></div>
    {{/if}}
    <label for="password">Password:</label>
    <input type="password" name="password" id="password" minlength="8" required maxlength="64" autocomplete="new-password">
  </div>

  <button type="submit">Login</button>
  <a href="/auth/register">Register</a>
</form>